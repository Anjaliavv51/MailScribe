# MailScribe
MailScribe is a Python-based NLP system that connects to Gmail, extracts email content, generates summaries (extractive &amp; transformer-based), and can optionally send intelligent auto-replies.
